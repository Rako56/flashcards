---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "(new repo) flashcards/package.json"
  - "(new repo) flashcards/tsconfig.json"
  - "(new repo) flashcards/.nvmrc"
  - "(new repo) flashcards/CLAUDE.md"
  - "(new repo) flashcards/.env.example"
  - "(new repo) flashcards/.gitignore"
  - "(new repo) flashcards/README.md"
  - "(new repo) flashcards/next.config.ts"
  - "(new repo) flashcards/tailwind.config.ts"
  - "(new repo) flashcards/postcss.config.mjs"
  - "(new repo) flashcards/components.json"
  - "(new repo) flashcards/app/layout.tsx"
  - "(new repo) flashcards/app/page.tsx"
  - "(new repo) flashcards/app/error.tsx"
  - "(new repo) flashcards/app/global-error.tsx"
  - "(new repo) flashcards/app/not-found.tsx"
  - "(new repo) flashcards/app/globals.css"
  - "(new repo) flashcards/app/providers.tsx"
  - "(new repo) flashcards/app/(marketing)/layout.tsx"
  - "(new repo) flashcards/app/(app)/layout.tsx"
  - "(new repo) flashcards/app/(admin)/layout.tsx"
  - "(new repo) flashcards/app/(auth)/layout.tsx"
  - "(new repo) flashcards/lib/utils.ts"
  - "(new repo) flashcards/lib/env.ts"
autonomous: false
requirements: [FOUND-01]
user_setup:
  - service: github
    why: "Repository host"
    dashboard_config:
      - task: "Create empty private repo `Rako56/flashcards` (or per Rafael decision)"
        location: "https://github.com/new"

must_haves:
  truths:
    - "`pnpm dev` starts a dev server on :3000 with no errors"
    - "`pnpm typecheck` exits 0 with strict 100% + 3 additional flags"
    - "Route groups (marketing), (app), (admin), (auth) exist as directories"
    - "`lib/env.ts` validates `process.env` via Zod and throws on missing required vars"
    - "New `CLAUDE.md` reflects reboot reality (no 'Sparkle', no legacy directives)"
  artifacts:
    - path: "flashcards/package.json"
      provides: "pnpm + engines + scripts"
      contains: "\"packageManager\": \"pnpm@9.15.9\""
    - path: "flashcards/tsconfig.json"
      provides: "TypeScript strict 100% + 3 flags"
      contains: "\"strict\": true"
    - path: "flashcards/.nvmrc"
      provides: "Node version pin"
      contains: "20.18"
    - path: "flashcards/lib/env.ts"
      provides: "Zod-validated server env"
      exports: ["env", "ServerEnv"]
    - path: "flashcards/app/(marketing)/layout.tsx"
      provides: "Marketing route group chrome"
    - path: "flashcards/app/(app)/layout.tsx"
      provides: "App route group chrome"
    - path: "flashcards/app/(admin)/layout.tsx"
      provides: "Admin route group chrome"
    - path: "flashcards/app/(auth)/layout.tsx"
      provides: "Auth route group chrome"
    - path: "flashcards/CLAUDE.md"
      provides: "Reboot-specific Claude directives (NOT legacy)"
      contains: "Flashcards"
  key_links:
    - from: "flashcards/app/layout.tsx"
      to: "flashcards/app/providers.tsx"
      via: "import + JSX wrap"
      pattern: "QueryClientProvider"
    - from: "flashcards/app/providers.tsx"
      to: "@tanstack/react-query"
      via: "QueryClient instance"
      pattern: "new QueryClient"
---

<objective>
Create the new `flashcards` repository on GitHub, scaffold Next.js 15.5 + App Router + TypeScript strict 100% + pnpm + Tailwind v3 + shadcn/ui v2, lay down the 4 route group placeholders, the Zod env validator, the cn() utility, the root layout/error/not-found shells, the new (reboot) CLAUDE.md, and `.env.example` + `.gitignore` + `README.md`. Install core deps (react-hook-form, zod, tanstack-query, clsx, tailwind-merge, tailwindcss-animate, @tailwindcss/typography) AND prove they are used (no decoration antipattern from legacy).

Purpose: Phase 1 plan 1 establishes the **executable substrate**. Every subsequent plan modifies files inside this scaffold. If this plan ships clean, Plans 1.2-1.10 build on a known good base. If this plan ships with `: any` casts or a missing `tsconfig` flag, every later plan inherits the rot.

Output: A new GitHub repo `Rako56/flashcards` (or per Rafael's choice) with a Next.js 15.5 scaffold that `pnpm dev` starts cleanly, `pnpm typecheck` passes, and route groups are visible in `app/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-SKELETON.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Key technical decisions from RESEARCH.md §Standard Stack + Code Examples 1, 2, 7. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

Version pins (all verified against npm registry 2026-05-21):
- next: 15.5.18 (exact)
- typescript: 5.7.3 (exact, in 5.7.x line)
- pnpm: 9.15.9 (exact, via packageManager field)
- node: 20.18.0 (exact, via .nvmrc + engines)
- react + react-dom: 19.0.0 (ships with Next 15.5)
- @supabase/ssr: 0.10.3
- @supabase/supabase-js: 2.106.1
- react-hook-form: ^7.76.0
- @hookform/resolvers: ^5.2.2
- zod: ^3.25.76 (NOT v4)
- @tanstack/react-query: ^5.100.11
- tailwindcss: 3.4.17 (NOT v4)
- tailwindcss-animate: ^1.0.7
- @tailwindcss/typography: ^0.5.16
- clsx: latest
- tailwind-merge: latest
- shadcn CLI: 2.3.0 (slate base color, CSS vars)

Required tsconfig.json compiler options (from RESEARCH.md Pattern 1):
- strict: true
- noUncheckedIndexedAccess: true
- exactOptionalPropertyTypes: true
- noPropertyAccessFromIndexSignature: true
- noImplicitReturns: true
- noFallthroughCasesInSwitch: true
- noImplicitOverride: true
- noUnusedLocals: true
- noUnusedParameters: true
- target: ES2022
- module: esnext
- moduleResolution: bundler
- jsx: preserve
- verbatimModuleSyntax: true
- paths: { "@/*": ["./*"] }

Required package.json scripts (from RESEARCH.md Example 1):
- dev: "next dev"
- build: "next build"
- start: "next start"
- typecheck: "tsc --noEmit"
- prepare: "husky" (added in 1.2)

ANTI-features (CLAUDE.md MUST forbid):
- IA visível ao aluno (no "gerar flashcards", "explicar com IA", "transcrever áudio")
- Tiptap rich-text editor
- Free tier amplo
- Multi-idioma (PT-BR only)
- Subscription auto-renewable
- Geração automática de conteúdo
- "Sparkle" branding (use "Flashcards" only)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → repo | First commit must establish convention; subsequent contributors inherit |
| process.env → server code | env vars may be unset/empty in misconfigured environments |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1.1-01 | Information Disclosure | `.env.local` accidentally committed | HIGH | mitigate | `.gitignore` excludes `.env*` (allow `.env.example`); `.env.example` documents vars WITHOUT secret values |
| T-1.1-02 | Tampering | Missing tsconfig strict flag = silent type erosion | HIGH | mitigate | `tsconfig.json` ships with `strict: true` + 3 flags in a SINGLE file (no parallel tsconfig.strict.json); pre-commit hook in 1.2 enforces |
| T-1.1-03 | Tampering | Wrong package versions installed (lockfile drift) | MEDIUM | mitigate | `packageManager: "pnpm@9.15.9"` + `engines.node` + `pnpm install --frozen-lockfile` in CI (added 1.4) |
| T-1.1-04 | Repudiation | New repo missing CLAUDE.md → Claude follows legacy conventions | HIGH | mitigate | Reboot-specific CLAUDE.md committed in Task 6 with explicit anti-features list |
| T-1.1-SC | Tampering | npm/pnpm install of malicious package | HIGH | mitigate | RESEARCH.md §Package Legitimacy Audit lists each package + source verification; all packages installed in 1.1 are [VERIFIED: official] from npm registry; no [ASSUMED]/[SUS] in this plan |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Decide GitHub repo location + Supabase project name</name>
  <decision>Which GitHub account/org hosts the new `flashcards` repo, and what is the Supabase project name?</decision>
  <context>RESEARCH.md A1+A2 + Open Question 1. Default recommendation: `Rako56/flashcards` (personal) + Supabase project name `flashcards-prod`. Migrating to org costs ~5min later. Rafael may have an existing org for flashcards.</context>
  <options>
    <option id="default">
      <name>Default: Rako56/flashcards + flashcards-prod</name>
      <pros>Fastest path; Rako56 has GitHub Pro; can migrate to org later</pros>
      <cons>None significant</cons>
    </option>
    <option id="org">
      <name>New org: e.g. flashcards-br/flashcards + flashcards-prod</name>
      <pros>Cleaner long-term ownership; org-level billing</pros>
      <cons>Adds ~5min setup; needs Rafael decision now</cons>
    </option>
  </options>
  <resume-signal>Reply: "default" OR provide `{ github: owner/name, supabase_project_name: name }` JSON</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create empty private GitHub repo</name>
  <files>(external — GitHub web UI)</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (§Open Question 1)
  </read_first>
  <action>Rafael creates the GitHub repo on https://github.com/new with these settings: name per Task 0 decision (default `flashcards`); visibility Private; **DO NOT** initialize with README/license/.gitignore (this plan ships those files in commit 1); description "Marketplace de preparações curadas para concursos públicos brasileiros". After creation, copy the SSH/HTTPS clone URL and post it as the resume signal.</action>
  <acceptance_criteria>
    - `gh repo view <owner>/<name>` returns 200 with `visibility: private`
    - Repo is empty (no commits, no default branch yet)
    - Rafael has admin permissions
  </acceptance_criteria>
  <verify>
    <human-check>Rafael confirms repo URL posted in resume signal; CC verifies via `gh repo view <owner>/<name>` returns 200.</human-check>
  </verify>
  <done>Repo exists, is empty, Rafael has admin, CC has SSH/HTTPS URL.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold Next.js 15.5 + TypeScript strict + pnpm + Tailwind + shadcn init</name>
  <files>flashcards/package.json, flashcards/tsconfig.json, flashcards/.nvmrc, flashcards/next.config.ts, flashcards/tailwind.config.ts, flashcards/postcss.config.mjs, flashcards/components.json, flashcards/app/layout.tsx, flashcards/app/page.tsx, flashcards/app/globals.css, flashcards/.gitignore, flashcards/.env.example, flashcards/README.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (§Standard Stack + §Installation + §Recommended Project Structure + Pattern 1 + Example 1 + Example 7)
    - .planning/research/STACK.md (§1 Next.js pin + §2 TS strict + §4 Tailwind v3 + §13 pnpm)
  </read_first>
  <action>
    Working in a parent dir (e.g. `C:/Users/Gamer/Documents/`), run `git clone <repo-url> flashcards-app && cd flashcards-app`. Run `pnpm dlx create-next-app@15.5.18 . --typescript --eslint --tailwind --app --import-alias "@/*" --no-src-dir` (note: NOT src dir per RESEARCH.md §Recommended Project Structure — top-level `app/`, `lib/`, `tests/`).

    Edit `package.json`: set `"packageManager": "pnpm@9.15.9"`, add `"engines": { "node": ">=20.18.0 <23" }`, add `"description"`, add scripts per RESEARCH.md Example 1 (dev, build, start, typecheck — others added by later plans). Pin `next` to exact `15.5.18`, `typescript` to `5.7.3`, `react`+`react-dom` to `19.0.0`. Run `pnpm view next@15.5 version` first to confirm 15.5.18 is still the latest 15.5.x patch — if higher patch exists (e.g. 15.5.19), accept the higher patch but document the actual version in commit message.

    Write `.nvmrc` with content `20.18.0` (no trailing newline issues; just the version).

    Overwrite `tsconfig.json` with the FULL strict config from RESEARCH.md Pattern 1 (verbatim — strict + the 3 flags + noImplicitReturns + noFallthroughCasesInSwitch + noImplicitOverride + noUnusedLocals + noUnusedParameters + target ES2022 + verbatimModuleSyntax + paths `@/*`).

    Verify Tailwind v3 (NOT v4): `pnpm list tailwindcss` shows `3.4.x`. Install plugins: `pnpm add -D tailwindcss-animate@^1.0.7 @tailwindcss/typography@^0.5.16`. Edit `tailwind.config.ts` to enable both plugins. Edit `app/globals.css` to add Tailwind base/components/utilities layers + CSS var slots for shadcn (use shadcn init defaults).

    Run `pnpm dlx shadcn@2.3.0 init`. Interactive answers: style=default, baseColor=slate, cssVariables=yes, tailwindConfig=tailwind.config.ts, tsxOnly=yes. Verify `components.json` exists.

    Run `pnpm view shadcn@2.3 version` first; if 2.3 deprecated, use latest 2.x but document version.

    Write `.gitignore` per RESEARCH.md Example 7 (verbatim, including the comment that `types/database.types.ts` MUST be committed).

    Write `.env.example` documenting (NO real values): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_PROJECT_ID`, `SUPABASE_ACCESS_TOKEN`, `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_AUTH_TOKEN`, `NEXT_PUBLIC_ROOT_DOMAIN=flashcards.com.br`, `NEXT_PUBLIC_APP_URL=https://flashcards.com.br`, `ASAAS_API_URL=https://sandbox.asaas.com/api/v3`, `ASAAS_API_KEY=`, `ASAAS_WEBHOOK_TOKEN=`, `RESEND_API_KEY=`, `LOG_LEVEL=info`. Each var gets a `# comment` explaining purpose + which Plan introduces it.

    Write `README.md` (minimal): project name, 1-paragraph description from PROJECT.md "What This Is", quickstart (`pnpm install` → `cp .env.example .env.local` → `pnpm dev`), link to `.planning/PROJECT.md`. Do NOT include "Sparkle" anywhere.

    Overwrite the default `app/page.tsx` with a minimal placeholder: `export default function HomePage() { return <main className="flex min-h-screen items-center justify-center p-8"><h1 className="text-3xl font-semibold">Flashcards — em construção</h1></main> }` and `app/layout.tsx` per Next 15.5 RSC convention (lang="pt-BR", metadata title "Flashcards", description "Marketplace de preparações curadas").
  </action>
  <acceptance_criteria>
    - `pnpm install` exits 0 (frozen lockfile not yet required)
    - `pnpm dev` starts a dev server on :3000 within 30s; `curl http://localhost:3000` returns HTML containing "Flashcards"
    - `pnpm typecheck` exits 0 with strict + 3 flags
    - `pnpm build` exits 0 (production build succeeds)
    - `cat package.json | grep packageManager` returns `"packageManager": "pnpm@9.15.9"`
    - `cat tsconfig.json | grep noUncheckedIndexedAccess` returns `"noUncheckedIndexedAccess": true`
    - `cat tsconfig.json | grep exactOptionalPropertyTypes` returns `"exactOptionalPropertyTypes": true`
    - `cat tsconfig.json | grep noPropertyAccessFromIndexSignature` returns `"noPropertyAccessFromIndexSignature": true`
    - `cat .nvmrc` returns `20.18.0`
    - `ls components.json` exists
    - `pnpm list tailwindcss` shows version starting with `3.4.`
    - `grep -r "Sparkle" .` returns no matches in any file (case-insensitive: `grep -ri "sparkle" --include='*.{ts,tsx,json,md}'`)
  </acceptance_criteria>
  <verify>
    <automated>cd flashcards-app && pnpm install && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>Scaffold compiles strict; dev server starts; no Sparkle traces.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create route group placeholders + error/not-found shells + providers</name>
  <files>flashcards/app/(marketing)/layout.tsx, flashcards/app/(app)/layout.tsx, flashcards/app/(admin)/layout.tsx, flashcards/app/(auth)/layout.tsx, flashcards/app/error.tsx, flashcards/app/global-error.tsx, flashcards/app/not-found.tsx, flashcards/app/providers.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (§Recommended Project Structure, lines describing route groups)
    - .planning/research/ARCHITECTURE.md (§Route groups section)
  </read_first>
  <action>
    Install runtime deps that providers + utilities need: `pnpm add @tanstack/react-query@^5.100.11 clsx tailwind-merge react-hook-form@^7.76.0 @hookform/resolvers@^5.2.2 zod@^3.25.76`.

    Create `app/providers.tsx` (Client Component). Export `function Providers({ children }: { children: React.ReactNode })` that wraps children in `QueryClientProvider` with a singleton `QueryClient` (defaultOptions: queries.staleTime 60_000, queries.refetchOnWindowFocus false). Mark with `'use client'`.

    Edit `app/layout.tsx` to wrap `<body>` children in `<Providers>`.

    Create the 4 route group layouts:
    - `app/(marketing)/layout.tsx` — Server Component, exports `default function MarketingLayout({ children }) { return <div data-surface="marketing">{children}</div> }`
    - `app/(app)/layout.tsx` — Server Component, exports `default function AppLayout({ children }) { return <div data-surface="app">{children}</div> }`. Add a comment: `// P3 will add auth guard + PrepPaywall hook here`
    - `app/(admin)/layout.tsx` — Server Component, exports `default function AdminLayout({ children }) { return <div data-surface="admin">{children}</div> }`. Add a comment: `// P8 will add requireAdmin() guard here`
    - `app/(auth)/layout.tsx` — Server Component, exports `default function AuthLayout({ children }) { return <div data-surface="auth" className="flex min-h-screen items-center justify-center">{children}</div> }`

    Create `app/error.tsx` (Client Component, `'use client'`) — receives `error: Error & { digest?: string }` + `reset: () => void`, displays a fallback UI with the digest + reset button. Use shadcn `Button` if generated, otherwise a styled `<button>`.

    Create `app/global-error.tsx` (Client Component, `'use client'`) — must include `<html><body>` (it replaces the root layout on root-level errors). Same UI as `app/error.tsx`.

    Create `app/not-found.tsx` (Server Component) — minimal 404 with a link back to `/`.

    Do NOT add any `page.tsx` inside the route groups in this plan — they remain placeholder folders for routing structure. Phase 2 will add `(marketing)/page.tsx`, etc.
  </action>
  <acceptance_criteria>
    - `ls flashcards/app/\(marketing\)/layout.tsx` exists
    - `ls flashcards/app/\(app\)/layout.tsx` exists
    - `ls flashcards/app/\(admin\)/layout.tsx` exists
    - `ls flashcards/app/\(auth\)/layout.tsx` exists
    - `grep -l "QueryClientProvider" flashcards/app/providers.tsx` exists
    - `grep -l "'use client'" flashcards/app/providers.tsx` exists
    - `grep -l "'use client'" flashcards/app/error.tsx` exists
    - `grep -l "<html>" flashcards/app/global-error.tsx` exists
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0 (validates route groups are wired correctly by Next 15.5)
  </acceptance_criteria>
  <verify>
    <automated>cd flashcards-app && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>4 route groups, providers wraps root, error/not-found shells in place, build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement lib/env.ts (Zod) + lib/utils.ts (cn) with proof-of-use tests</name>
  <files>flashcards/lib/env.ts, flashcards/lib/utils.ts, flashcards/lib/__tests__/utils.test.ts, flashcards/lib/__tests__/env.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (§Example 2 lib/env.ts)
  </read_first>
  <behavior>
    Test 1 (`lib/__tests__/utils.test.ts`): `cn('a', 'b', false && 'c', { d: true, e: false })` returns `"a b d"` (clsx behavior); `cn('p-2', 'p-4')` returns `"p-4"` (tailwind-merge precedence).
    Test 2 (`lib/__tests__/env.test.ts`): calling `env()` with `process.env` missing `NEXT_PUBLIC_SUPABASE_URL` THROWS with an error message containing `NEXT_PUBLIC_SUPABASE_URL`. Calling `env()` with all required vars present returns a typed object with `NEXT_PUBLIC_SUPABASE_URL` as `string`. Test uses `vi.stubEnv` per Vitest 3.2 docs (will run after Plan 1.3 installs Vitest; for now, write tests against `@types/vitest` and they will be runnable after 1.3).
    Test 3 (`lib/__tests__/env.test.ts`): `LOG_LEVEL` defaults to `info` when unset; `NEXT_PUBLIC_ROOT_DOMAIN` defaults to `flashcards.com.br`.
  </behavior>
  <action>
    Create `lib/utils.ts` exporting `cn(...inputs: ClassValue[]): string` = `twMerge(clsx(...inputs))` (per shadcn convention). Import `clsx` from `clsx` and `twMerge` from `tailwind-merge`. Add JSDoc comment explaining: "Use cn() for ALL conditional Tailwind classes; never concatenate template strings."

    Create `lib/env.ts` per RESEARCH.md Example 2 verbatim — `serverEnvSchema` Zod object with all 14 env vars (Supabase 3, Sentry 2, Domain 2, Logging 3, Asaas 3, Resend 1). Use `.optional()` for placeholders (Asaas/Sentry/Resend tokens — they ship empty in P1 but get values in P4/P9). Export `ServerEnv` type via `z.infer`. Export `env()` function with lazy caching pattern (per Example 2). On parse failure, throw a descriptive error containing the field names that failed.

    Create the 2 test files. They will not yet RUN (Vitest installed in 1.3) but they must TYPECHECK now. Use `// @ts-ignore -- vitest installed in Plan 1.3` directives at the top if needed to suppress import errors temporarily, BUT prefer installing `vitest@3.2.4` as a devDep RIGHT NOW (`pnpm add -D vitest@3.2.4`) so types resolve without ignores. The full Vitest config (vitest.config.ts) comes in Plan 1.3.

    Verify cn works at runtime by manually importing in `app/page.tsx`: `import { cn } from '@/lib/utils'` and using `cn('text-3xl', 'font-semibold')` for the H1 className. This proves cn is "used" (anti-decoration).
  </action>
  <acceptance_criteria>
    - `lib/utils.ts` exports `cn` function
    - `lib/env.ts` exports `env()` function AND `ServerEnv` type
    - `lib/env.ts` imports `z` from `zod` (proof zod is wired, not decoration)
    - `app/page.tsx` imports `cn` from `@/lib/utils` (proof clsx + tailwind-merge are wired)
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
    - `cat lib/env.ts | grep -c "z\\.string\\|z\\.enum\\|z\\.object"` returns at least 3 (proves Zod schema is non-trivial)
  </acceptance_criteria>
  <verify>
    <automated>cd flashcards-app && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>env.ts validates required vars; cn() works and is used; tests written (run in P1.3).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Write new (reboot) CLAUDE.md with anti-features and reboot directives</name>
  <files>flashcards/CLAUDE.md</files>
  <read_first>
    - .planning/PROJECT.md (Constraints, Key Decisions, Out of Scope)
    - .planning/REQUIREMENTS.md (Out of Scope table)
    - .planning/phases/01-foundation/01-RESEARCH.md (§Project Constraints from CLAUDE.md + §Locked Decisions)
  </read_first>
  <action>
    Write a NEW `CLAUDE.md` at the repo root with the following sections (replacing/superseding the legacy `sparkle-study-scape/CLAUDE.md` entirely):

    **Section "Project Identity":** Name=Flashcards, Domain=flashcards.com.br, Stack=Next.js 15.5 + TS strict + Supabase Pro + Asaas + Vercel Pro. Explicitly forbid the words "Sparkle", "Sparkle Flashcards", "Sparkle Study Scape" anywhere in code/comments/copy.

    **Section "Anti-Features (NEVER ship)":** Bulleted list — IA visível ao aluno; Tiptap rich-text editor; free tier amplo; multi-idioma; subscription auto-renewable; geração automática de conteúdo; native mobile v1; OCR PDF upload pelo aluno; DIY deck editor; push notifications de guilt; gamification spam; multi-tier pricing; Stripe ou outro gateway; chat IA/suporte IA-first.

    **Section "Tech Pins (immutable for v1)":** Next 15.5.x (NOT 16), TS 5.7.x strict 100%, pnpm 9.15.x, Node 20.18.x, Supabase Pro (sa-east-1), Tailwind v3 (NOT v4), Zod v3 (NOT v4), ESLint v9 flat, Vitest 3.2.x, Sentry @sentry/nextjs 10.53.x, pino v9.

    **Section "Quality Gates (non-negotiable)":** TS strict 100% (no `: any` / `as any` — ESLint error); ESLint CI blocking; coverage gates (≥50% global, ≥90% on `lib/srs/`, `lib/queue/`, `lib/asaas/`, `lib/access/`); Sentry + pino from commit 1; atomic Postgres functions for read-modify-write; RLS on every table; `getConcursoBySlug()` is the only API to a concurso (hardcoded UUIDs banned).

    **Section "Conventions":** Use `@/` import alias (no deep relative paths); `cn()` from `@/lib/utils` for conditional Tailwind; prefer shadcn/ui primitives extended via `components/ui/`; Tests Vitest + RTL + MSW + Playwright; no legacy DB carryover.

    **Section "Service-Role Isolation":** `lib/supabase/admin.ts` ALWAYS imports `'server-only'` + runtime `throw if typeof window !== 'undefined'`; service_role key NEVER in `NEXT_PUBLIC_*`; ESLint blocks misuse.

    **Section "Webhook Discipline":** Webhook returns 500 on transient errors (DB down, Asaas re-fetch fail) so Asaas retenta; returns 200 only on success or unrecoverable. Every catch in Route Handlers/Server Actions MUST call `Sentry.captureException`. Legacy returned 200 on every error — that's banned.

    **Section "Reference":** Link to `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/research/STACK.md`, `.planning/research/ARCHITECTURE.md`.

    Do NOT include any legacy directives (no references to Vite, Lovable, the legacy Supabase project `zjyogswbgcauwqisvuyq`, or legacy file paths).
  </action>
  <acceptance_criteria>
    - `flashcards/CLAUDE.md` exists
    - File contains "Flashcards" but NOT "Sparkle" (case-insensitive): `grep -ic "sparkle" CLAUDE.md` returns 0 (only allowed mention is in "forbidden words" enumeration if listing what NOT to use)
    - File mentions all anti-features from REQUIREMENTS.md Out of Scope (at minimum: AI gen, Tiptap, free tier, multi-idioma, auto-renew)
    - File mentions version pins: Next 15.5, TS 5.7, pnpm 9.15, Node 20.18, Supabase Pro, Tailwind v3, Zod v3
    - File mentions service-role isolation rule
    - File mentions webhook 500-vs-200 discipline
  </acceptance_criteria>
  <verify>
    <automated>cd flashcards-app && test -f CLAUDE.md && grep -q "Flashcards" CLAUDE.md && grep -q "service_role" CLAUDE.md && grep -q "15.5" CLAUDE.md && [ $(grep -ic "sparkle" CLAUDE.md) -le 1 ]</automated>
  </verify>
  <done>New CLAUDE.md committed; no legacy contamination; all critical pins + anti-features documented.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Initial commit + push + verify CI-readiness (no CI yet, just baseline)</name>
  <files>(git operation — no new files)</files>
  <read_first>
    - flashcards/.gitignore (verify contents)
    - flashcards/package.json (verify scripts)
  </read_first>
  <action>
    From `flashcards-app/`: `git add -A` (verify with `git status` no `.env.local` shows up — should be gitignored). Commit: `feat: scaffold Next.js 15.5 + TS strict + pnpm + Tailwind v3 + shadcn (Plan 1.1)`. Use heredoc multi-line commit message:
    - Line 1: `feat: scaffold Next.js 15.5 + TS strict + pnpm + Tailwind v3 + shadcn (Plan 1.1)`
    - Blank
    - Body: bullet list of what shipped (Next 15.5.18, TS 5.7.3 strict + 3 flags, pnpm 9.15.9, 4 route groups, lib/env.ts + lib/utils.ts, reboot CLAUDE.md, env.example, gitignore).

    Push: `git push -u origin main` (Plan 1.4 will add branch protection — for THIS first commit, direct push to main is required because no PR can land before CI exists).

    Verify on GitHub: `gh repo view --web` shows the commit; `gh api repos/<owner>/<name>/commits/main --jq '.sha'` returns a non-empty SHA.

    Sanity probe: in a fresh terminal, `cd <tmp>; git clone <repo-url> tmp-flashcards && cd tmp-flashcards && pnpm install --frozen-lockfile && pnpm typecheck && pnpm build` — must all pass. (This proves the repo is self-bootstrapping.)
  </action>
  <acceptance_criteria>
    - `gh api repos/<owner>/<name>/commits/main --jq '.sha'` returns a non-empty SHA
    - `git log --oneline -1` shows the Plan 1.1 commit
    - Fresh clone + `pnpm install --frozen-lockfile` + `pnpm typecheck` + `pnpm build` ALL exit 0 in a clean directory
    - `gh api repos/<owner>/<name> --jq '.default_branch'` returns `main`
    - `gh api repos/<owner>/<name> --jq '.private'` returns `true`
  </acceptance_criteria>
  <verify>
    <automated>git -C flashcards-app log --oneline -1 && gh api repos/${GH_OWNER}/${GH_REPO}/commits/main --jq '.sha'</automated>
  </verify>
  <done>First commit on `main`, reproducible from fresh clone, repo is self-bootstrapping.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `cd flashcards-app && pnpm dev` — server starts on :3000 within 30s, homepage renders "Flashcards — em construção"
2. `pnpm typecheck` — strict 100% + 3 flags, exits 0
3. `pnpm build` — production build succeeds
4. `ls flashcards-app/app` shows: `(marketing)/`, `(app)/`, `(admin)/`, `(auth)/`, `layout.tsx`, `page.tsx`, `providers.tsx`, `globals.css`, `error.tsx`, `global-error.tsx`, `not-found.tsx`
5. `ls flashcards-app/lib` shows: `env.ts`, `utils.ts`, `__tests__/`
6. `cat flashcards-app/CLAUDE.md` shows reboot directives; `grep -ic "sparkle" CLAUDE.md` ≤ 1
7. `git log --oneline -1` on `main` shows the Plan 1.1 commit
8. Fresh clone in /tmp + install + typecheck + build all exit 0

Manual checks:
- Rafael confirms GitHub repo location matches the Task 0 decision
- Rafael confirms `pnpm dev` shows homepage with no "Sparkle" anywhere
</verification>

<success_criteria>
- All `must_haves.truths` are observably true
- All `must_haves.artifacts` exist with the specified contents/exports
- `must_haves.key_links` are wired (Providers wraps root layout)
- `pnpm install --frozen-lockfile && pnpm typecheck && pnpm build` exits 0 from a fresh clone
- New repo on GitHub at `<owner>/flashcards` with first commit on `main`
- Zero "Sparkle" leaks
- FOUND-01 satisfied (Next.js 15.5 + App Router + TS strict 100% scaffold, pnpm, Node 20.18.x in .nvmrc)
</success_criteria>

<rafael_actions_required>
1. **Task 0:** Decide repo location (default: `Rako56/flashcards`) + Supabase project name (default: `flashcards-prod`)
2. **Task 1:** Create empty private GitHub repo per the decision; post clone URL as resume signal

Everything else is autonomous Claude work.
</rafael_actions_required>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done with:
- Final package versions installed (resolved from pnpm-lock.yaml)
- Repo URL + first commit SHA
- Any deviations from the plan (e.g. minor patch bumps accepted)
- Confirmation that `pnpm dev` + `pnpm typecheck` + `pnpm build` all pass
- Confirmation that no "Sparkle" residue exists
</output>
