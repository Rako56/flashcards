---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - flashcards/playwright.config.ts
  - flashcards/tests/e2e/.gitkeep
  - flashcards/package.json
autonomous: true
requirements: [FOUND-06]

must_haves:
  truths:
    - "`pnpm test:e2e` runs Playwright Chromium against PLAYWRIGHT_BASE_URL successfully (or against http://localhost:3000 fallback)"
    - "Playwright Chromium browser installed"
    - "`playwright.config.ts` exists with PLAYWRIGHT_BASE_URL support (Vercel preview deploy URL via env)"
    - "An intentionally-uncovered file in lib/srs/ causes `pnpm test --coverage` to FAIL with threshold error (gate-break probe proves per-path threshold actually enforces)"
    - "After the probe, the uncovered file is removed and coverage gate passes again"
  artifacts:
    - path: "flashcards/playwright.config.ts"
      provides: "E2E with PLAYWRIGHT_BASE_URL support"
      contains: "PLAYWRIGHT_BASE_URL"
    - path: "flashcards/tests/e2e/.gitkeep"
      provides: "Tracks the e2e directory; smoke test ships in Plan 01-13"
  key_links:
    - from: "flashcards/playwright.config.ts"
      to: "PLAYWRIGHT_BASE_URL env (set in CI to Vercel preview URL)"
      via: "baseURL config"
      pattern: "PLAYWRIGHT_BASE_URL"
---

<objective>
Install Playwright 1.60.0 + Chromium browser; write `playwright.config.ts` targeting Vercel preview deploy URL via `PLAYWRIGHT_BASE_URL` env (with localhost fallback for local dev); run a one-time gate-break coverage probe: add an uncovered exported function in `lib/srs/`, verify `pnpm test --coverage` fails RED with threshold error, then revert.

Purpose: Per RESEARCH.md A7 — Vitest 3.2 per-path threshold shape is documented but must be PROVEN. This probe is the only way to know the per-path coverage gate actually fires when violated. Without this proof, the 90% gate is aspirational. Playwright config exists from day 1 so Plan 01-13 (smoke E2E) only adds the spec file.

Output: Playwright Chromium installed; `playwright.config.ts` parses; gate-break probe documented (showed `pnpm test --coverage` failed with `90%` mention before revert).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-SKELETON.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@flashcards/vitest.config.ts
@flashcards/package.json
@.planning/research/STACK.md

<interfaces>
<!-- Versions -->
- @playwright/test: 1.60.0 (exact pin per STACK.md)

<!-- Playwright config (RESEARCH.md Example 5 verbatim) -->
- testDir: './tests/e2e'
- fullyParallel: true
- forbidOnly: !!process.env.CI
- retries: process.env.CI ? 2 : 0
- workers: process.env.CI ? 1 : undefined
- reporter: process.env.CI ? [['github'], ['html']] : 'list'
- use.baseURL: process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:3000'
- use.trace, screenshot, video per Example 5
- projects: [chromium only in P1; mobile + safari deferred to P7 DESIGN-07]
- webServer: only when no PLAYWRIGHT_BASE_URL (local dev); CI uses Vercel preview URL

<!-- Gate-break probe shape (VALIDATION.md §Gate-Break Tests "Coverage 90% core") -->
Create `lib/srs/uncovered-temp.ts` with 10+ exported untested functions → run `pnpm test --coverage` → MUST exit 1 with threshold error mentioning `lib/srs` or `90%` → delete temp file → re-run `pnpm test --coverage` → MUST exit 0.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser → live network | Chromium downloads run from Microsoft CDN; verify SHA via Playwright CLI |
| Coverage probe artifact → main branch | Probe creates a temp file; if accidentally committed, would break CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1.4-01 | Tampering | Per-path coverage threshold silently downgraded by Vitest version | HIGH | mitigate | Gate-break probe in this plan PROVES the per-path threshold fires when violated; documented output in SUMMARY |
| T-1.4-02 | Repudiation | Probe temp file accidentally committed | MEDIUM | mitigate | Probe runs in a single shell session; cleanup step deletes file BEFORE any git operation; `git status` confirms clean tree at end |
| T-1.4-SC | Tampering | Playwright browser binary | LOW | mitigate | @playwright/test [VERIFIED: official Microsoft] per RESEARCH.md Audit; Chromium downloaded via `pnpm exec playwright install` (official channel) |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Playwright + Chromium browser</name>
  <files>flashcards/package.json, flashcards/pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (§Installation step 4)
    - flashcards/package.json
  </read_first>
  <action>
    From `flashcards-app/`:

    `pnpm add -D @playwright/test@1.60.0`

    Install Chromium only (webkit + firefox deferred to Phase 7 DESIGN-07):
    `pnpm exec playwright install chromium`

    Edit `package.json` scripts ADD: `test:e2e: "playwright test"`, `test:e2e:ui: "playwright test --ui"`.
  </action>
  <acceptance_criteria>
    - `pnpm list @playwright/test` shows `1.60.x`
    - `pnpm exec playwright --version` works and shows 1.60.x
    - `cat package.json | grep -c "\"test:e2e\"\\|\"test:e2e:ui\""` returns at least 2
  </acceptance_criteria>
  <verify>
    <automated>cd flashcards-app && pnpm list @playwright/test 2>&1 | grep -E "playwright/test@1.60" && pnpm exec playwright --version</automated>
  </verify>
  <done>Playwright + Chromium installed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write playwright.config.ts + tests/e2e/ scaffold</name>
  <files>flashcards/playwright.config.ts, flashcards/tests/e2e/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (§Code Examples §Example 5 playwright.config.ts verbatim)
  </read_first>
  <action>
    Write `playwright.config.ts` per RESEARCH.md Example 5 verbatim:
    - testDir: `'./tests/e2e'`
    - fullyParallel: true
    - forbidOnly: `!!process.env.CI`
    - retries: `process.env.CI ? 2 : 0`
    - workers: `process.env.CI ? 1 : undefined`
    - reporter: `process.env.CI ? [['github'], ['html']] : 'list'`
    - use.baseURL: `process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:3000'`
    - use.trace, screenshot, video per Example 5
    - projects: `[{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }]` (only chromium — mobile + safari deferred to P7 DESIGN-07)
    - webServer: `process.env.PLAYWRIGHT_BASE_URL ? undefined : { command: 'pnpm dev', url: baseURL, reuseExistingServer: !process.env.CI, timeout: 120_000 }`

    Create `tests/e2e/.gitkeep` (empty file) so the directory is tracked. The actual smoke test (`tests/e2e/smoke.spec.ts`) lands in Plan 01-13.

    Verify config compiles: `pnpm exec playwright test --list` exits 0 (no tests yet, but config must parse).
  </action>
  <acceptance_criteria>
    - `ls playwright.config.ts` exists
    - `grep "PLAYWRIGHT_BASE_URL" playwright.config.ts` returns at least 1
    - `grep "chromium" playwright.config.ts` returns at least 1
    - `grep "webServer" playwright.config.ts` returns at least 1
    - `ls tests/e2e/.gitkeep` exists
    - `pnpm exec playwright test --list` exits 0 (parsing succeeds even with no tests)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd flashcards-app && pnpm typecheck && pnpm exec playwright test --list</automated>
  </verify>
  <done>Playwright config in place; chromium project configured; webServer for local + baseURL for CI.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Gate-break coverage probe — prove per-directory threshold actually enforces</name>
  <files>(transient — verifies gate fires, no permanent file)</files>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (§Gate-Break Tests — "Coverage 90% core" row)
    - .planning/phases/01-foundation/01-RESEARCH.md (§Assumption A7)
    - flashcards/vitest.config.ts
  </read_first>
  <action>
    Per RESEARCH.md A7 — Vitest 3.2 per-path threshold shape is documented but must be PROVEN.

    Step 1 — Create a temp file `lib/srs/uncovered-temp.ts` with 11 exported untested functions (each function = one line return statement; goal: drop lib/srs/ coverage below 90% by adding lots of uncovered statements).

    Step 2 — Run `pnpm test --coverage`. EXPECTED: exits 1 with threshold error like "Coverage for lines (X%) does not meet threshold (90%) for lib/srs/**". Capture the exact error message.

    Step 3 — If exit code is 0 (gate didn't fire), it means the per-path threshold syntax is wrong for Vitest 3.2.4:
    1. Open the Vitest docs for the installed version (`pnpm exec vitest --version`)
    2. Adjust `vitest.config.ts` to the correct shape
    3. Re-run probe
    4. Document the actual working shape in the plan SUMMARY

    Step 4 — Once the gate fires correctly, DELETE `lib/srs/uncovered-temp.ts` and re-run `pnpm test --coverage` — must exit 0 with thresholds met.

    Step 5 — Document the probe output (the threshold failure error message) in the plan SUMMARY for posterity.

    Step 6 — Confirm `git status` is clean (no probe file leaked).
  </action>
  <acceptance_criteria>
    - With `lib/srs/uncovered-temp.ts` present, `pnpm test --coverage` exits 1 with threshold error
    - Error message mentions `lib/srs` or `90%` (proves per-path engaged)
    - After deleting the temp file, `pnpm test --coverage` exits 0
    - `git status` shows clean tree (no probe file leaked)
    - Plan SUMMARY documents the verified per-path threshold syntax + the exact error message observed
  </acceptance_criteria>
  <verify>
    <automated>cd flashcards-app && printf 'export const x = 1;\nexport function f1(){return 1}\nexport function f2(){return 2}\nexport function f3(){return 3}\nexport function f4(){return 4}\nexport function f5(){return 5}\nexport function f6(){return 6}\nexport function f7(){return 7}\nexport function f8(){return 8}\nexport function f9(){return 9}\nexport function f10(){return 10}\nexport function f11(){return 11}\n' > lib/srs/uncovered-temp.ts && pnpm test --coverage; PROBE_EXIT=$?; rm lib/srs/uncovered-temp.ts && pnpm test --coverage && [ $PROBE_EXIT -ne 0 ]</automated>
  </verify>
  <done>Per-path coverage gate proven to fire; temp file removed; thresholds verified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Commit + push Plan 1.4</name>
  <files>(git operation)</files>
  <read_first>
    - flashcards/playwright.config.ts (verify in place)
  </read_first>
  <action>
    From `flashcards-app/`, stage: `git add playwright.config.ts tests/e2e/ package.json pnpm-lock.yaml`.

    Verify `git status` does NOT include `lib/srs/uncovered-temp.ts` (probe should have been cleaned up in Task 3).

    Verify pre-commit hook (from 1.2) passes: lint + typecheck must succeed.

    Commit: `feat: Playwright 1.60 + Chromium + gate-break probe verified (Plan 1.4)` with body listing: playwright.config.ts with PLAYWRIGHT_BASE_URL support, gate-break probe documented (exact error message).

    Push: `git push`.
  </action>
  <acceptance_criteria>
    - `git log --oneline -1` shows the Plan 1.4 commit
    - `git push` succeeded
    - `gh api repos/<owner>/<name>/commits/main --jq '.sha'` returns the new commit SHA
    - Probe file NOT in git history (verified by `git log --all --pretty=format: --name-only | grep -c "uncovered-temp.ts"` returns 0)
  </acceptance_criteria>
  <verify>
    <automated>cd flashcards-app && git log --oneline -1 | grep -q "Plan 1.4\\|Playwright\\|gate-break"</automated>
  </verify>
  <done>Plan 1.4 committed and pushed; gate-break probe documented in SUMMARY.</done>
</task>

</tasks>

<verification>
1. `pnpm exec playwright test --list` exits 0 (config parses, no tests yet)
2. `pnpm exec playwright --version` works (browsers installed)
3. Gate-break probe (uncovered temp file in lib/srs/) caused `pnpm test --coverage` to exit 1 (documented in SUMMARY)
4. After probe revert: `pnpm test --coverage` exits 0
5. `git status` clean after probe
</verification>

<success_criteria>
- All `must_haves.truths` are observably true
- Per-directory coverage threshold proven to fire (gate-break probe)
- Playwright Chromium installed and config compiles
- FOUND-06 satisfied (Playwright installed + configured E2E in CI; smoke spec lands in Plan 01-13)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` with:
- Final versions installed (playwright)
- Verified Vitest 3.2.4 per-path threshold syntax (from gate-break probe)
- Exact error message from the gate-break probe (proves threshold actually enforces)
- Commit SHA of Plan 1.4 commit
- Any tweaks needed to vitest.config.ts if per-path syntax differed from RESEARCH.md
</output>
</content>
</invoke>