import { cn } from '@/lib/utils'

export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <h1 className={cn('text-3xl', 'font-semibold')}>Flashcards — em construção</h1>
    </main>
  )
}
